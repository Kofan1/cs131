A1: ls -l > listing.txt
date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt
Standard error file descriptor number is 2.
A3: ls /nonexistent_dir /etc 1> output.txt 2> errors.txt
A4: ls /nonexistent_dir /etc &> combined.txt
A5: The terminal only showed the stdout which is "ls /etc", and no error message, since the message redirect to /dev/null. The /dev/null is a special file that it will discard anything written to it.
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: grep -c 'ERROR' server.log
A9: grep -c '^ERROR' server.log > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -nr
A11: grep -oE '"state":"[^"]+"' /data/yelp/yelp_academic_dataset_business.json | sort | uniq -c | sort -rn | head -5

B1:
B2:
B3:
B4:
B5: chmod  640 fruits.txt
B6: find . -type f -user $(whoami) -perm -u=w
